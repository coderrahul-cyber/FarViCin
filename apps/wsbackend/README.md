### WHAT TO FOR PRODUCTION DEPLOYMENT
*Use the redis for the pub/sub mechanism in production deployment. The default in-memory channel layer is not suitable for production as it does not support multiple processes or servers.*
